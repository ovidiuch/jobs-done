import { StatefulStep } from '../StatefulStep';
import { Step } from '..';

export default {
  component: Step,
  decorator: StatefulStep,
  bg: true,
  viewport: {
    width: 411,
    height: 731
  }
};
